{% set call_title = object.call.title if object.call else '' %}
{% set reviewer_name = review.reviewer.username if review.reviewer else '' %}
{% set status_de = {'approved': 'Angenommen', 'rejected': 'Abgelehnt', 'revise': 'Überarbeitung erbeten'}.get(review.status, review.status) %}
{% set status_en = {'approved': 'Approved', 'rejected': 'Rejected', 'revise': 'Revision requested'}.get(review.status, review.status) %}
[Deutsch]

Ein Gutachten ist eingegangen für die Einreichung "{{ object.title }}"{% if call_title %} in "{{ call_title }}"{% endif %}.

  Gutachter: {{ reviewer_name }}
  Ergebnis: {{ status_de }}
{% if review.comment %}
  Kommentar: {{ review.comment }}
{% endif %}

Die Einreichung ist unter folgendem Link einsehbar:

{{ proposal_url }}

================================

[English]

A review has been submitted for the submission "{{ object.title }}"{% if call_title %} in "{{ call_title }}"{% endif %}.

  Reviewer: {{ reviewer_name }}
  Result: {{ status_en }}
{% if review.comment %}
  Comment: {{ review.comment }}
{% endif %}

The submission can be viewed at the following link:

{{ proposal_url }}
